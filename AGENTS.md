# AGENTS.md

给代理的工作约定（随发现随补记）。

## 文档口径

- README.md 是写给**用户**的产品介绍：保持简洁，讲清「有什么、怎么用」即可；
  不写实现细节、技术要点与逐项参数。细节放代码注释或 doc/ 下的设计文档。

## UI 改动要有据可依

应用是 Tauri 专属，浏览器里跑不出书库——所以「界面别扭」这类判断若只靠脑补，
改完还是别扭。`tools/ui-preview/` 把原生层换成替身，真实组件可以在无头 Chrome 里
**截图与量盒**（起法见该目录 README）。改侧栏、卡片区这类版面时：

- 先截一张**改前**的图，把「别扭」量成数字（对齐线、行高、对比度），再动手；
- 改完用同一套判据复量一次，并跑 `npm run type-check` 与 `npx vitest run`；
- 视觉验收含三套主题（light / sepia / dark）——`--theme=` 可各截一张。

## 交互改动要真机复验，别信单次成功

凡是走浏览器**原生状态机**的交互（拖放、指针捕获、文本选择），合成事件与预览站都
验不了协议层：`tools/ui-preview/app-*.mjs` 才能把**打包好的桌面端**拉起来跑真实手势。

- 判据是**同一手势多跑几次稳不稳**，不是「跑通一次」：`app-drag-stability.mjs`
  连做 N 手，每次都挑一对会真正改变顺序的分组，于是「行序没变」只可能是手势丢了；
- HTML5 拖放**不要**用于应用内拖拽：在 Windows 的 WebView2 里 drop 会时有时无
  （`doc/sidebar-ux.md` 第六轮），关掉 Tauri 的拖放处理器更糟（第七轮）。应用内拖动
  一律走 Pointer Events，判定抽成纯函数放 `lib/`，手势放 `composables/`；
- 拖动要**整行可抓**、且行内控件不得对命中测试隐形（`pointer-events: none` 是 HTML5
  时代的遗留物，会把可拖区域切成几条缝，见第八轮）；侧栏静止时**不改光标**（整列常驻
  的条目一直握着小手太吵），可拖的提示交给拖动中的反馈；
- **不要在 `pointerdown` 就 `setPointerCapture`**：捕获会把 `pointerup` 重定向，而
  `click` 是按"按下与松开的最近公共祖先"派的——行内按钮会因此收不到 click（"分组点不开"
  就是这么来的）。捕获推迟到真正"拿起"那一刻；
- `click` 语义的单测别用 `el.click()`：那绕过了整条指针序列，会假绿。用真鼠标的探针
  （`app-click-probe.mjs`）验；
- 「拖不动」类问题先分清是**手势没起来**还是**拿起后没落子**：`app-grab-probe.mjs`
  看前者（三处抓，抓哪儿都该拿得起来），`app-drag-stability.mjs` 看后者；
- 探针里 `--trace=1` 会改变结果（页内捕获监听器本身就让丢的那次不丢）——trace 只看
  序列，判据用裸跑；
- 沙箱注意：`vite dev` / `vitest` / 无头 Chrome / 桌面端探针都会 spawn 带管道的子进程，
  受限沙箱下报 `spawn EPERM` / Mojo 拒绝访问，这些命令需要放宽文件权限执行。
